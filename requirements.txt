# SHIPS — Teradata Deployment Agent
# Runtime dependencies
#
# Core packages use Python standard library only.
# teradatasql is required only for live deployment (Ship phase).

teradatasql>=20.0.0

# HTTP transport support for the SHIPS MCP server (streamable-http and sse).
# Not required for stdio-only deployments, but included here so the MCP
# server works out of the box in enterprise environments without a separate
# install step.
uvicorn>=0.30.0

# JWT/Bearer authentication for the SHIPS MCP server (--auth-jwks-uri).
# Required when running with --transport streamable-http or sse and
# --auth-jwks-uri is configured. Not required for stdio or unauthenticated
# HTTP deployments.
PyJWT[crypto]>=2.8.0
httpx>=0.27.0
